{
  "name": "N8N Environment Variables Configuration",
  "description": "Variables de entorno necesarias para los workflows de OptimaCX",
  "variables": {
    "SUPABASE_URL": {
      "value": "https://your-project.supabase.co",
      "description": "URL base de Supabase",
      "type": "string",
      "required": true
    },
    "SUPABASE_ANON_KEY": {
      "value": "your-anon-key",
      "description": "Clave anónima de Supabase",
      "type": "string",
      "required": true,
      "secret": true
    },
    "SUPABASE_SERVICE_KEY": {
      "value": "your-service-key",
      "description": "Clave de servicio de Supabase",
      "type": "string",
      "required": true,
      "secret": true
    },
    "N8N_WEBHOOK_BASE_URL": {
      "value": "https://your-n8n-instance.com",
      "description": "URL base para webhooks internos de N8N",
      "type": "string",
      "required": true
    },
    "WHATSAPP_TOKEN": {
      "value": "your-whatsapp-token",
      "description": "Token de autenticación de WhatsApp Business API",
      "type": "string",
      "required": true,
      "secret": true
    },
    "WHATSAPP_PHONE_ID": {
      "value": "your-phone-number-id",
      "description": "ID del número de teléfono de WhatsApp Business",
      "type": "string",
      "required": true
    },
    "OPENAI_API_KEY": {
      "value": "your-openai-key",
      "description": "Clave API de OpenAI para análisis de IA",
      "type": "string",
      "required": false,
      "secret": true
    },
    "GEMINI_API_KEY": {
      "value": "your-gemini-key",
      "description": "Clave API de Google Gemini",
      "type": "string",
      "required": false,
      "secret": true
    },
    "REDIS_URL": {
      "value": "redis://localhost:6379",
      "description": "URL de conexión a Redis para cache",
      "type": "string",
      "required": false
    },
    "SMTP_HOST": {
      "value": "smtp.gmail.com",
      "description": "Servidor SMTP para envío de emails",
      "type": "string",
      "required": true
    },
    "SMTP_PORT": {
      "value": "587",
      "description": "Puerto del servidor SMTP",
      "type": "number",
      "required": true
    },
    "SMTP_USER": {
      "value": "your-email@domain.com",
      "description": "Usuario SMTP",
      "type": "string",
      "required": true
    },
    "SMTP_PASSWORD": {
      "value": "your-smtp-password",
      "description": "Contraseña SMTP",
      "type": "string",
      "required": true,
      "secret": true
    }
  },
  "setup_instructions": [
    "1. Acceder a N8N admin panel",
    "2. Ir a Settings > Variables",
    "3. Crear cada variable listada arriba",
    "4. Configurar las credenciales necesarias",
    "5. Probar la conectividad de cada servicio"
  ],
  "notes": [
    "Las variables marcadas como 'secret' deben configurarse como credenciales en N8N",
    "Verificar que todas las URLs sean accesibles desde N8N",
    "Para producción, usar variables de entorno del sistema operativo"
  ]
}
